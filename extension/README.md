# Extension package

This directory is the unpacked Manifest V3 extension. For installation, architecture,
privacy guarantees, supported sites, and development commands, see the repository
[README](../README.md).

Load this directory, not the repository root, from `chrome://extensions`.
